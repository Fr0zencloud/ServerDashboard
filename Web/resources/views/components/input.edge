<input 
    id="input_{{ name }}"  
    type="{{ type ? type : 'text' }}"
    name="{{ name }}"
    @if(placeholder)
        placeholder="{{ placeholder }}"
    @endif
    @if(value)
        value="{{ value }}"
    @endif
    {{ required ? 'required' : null }}
>